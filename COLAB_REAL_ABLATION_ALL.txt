# ===== COLAB A — REAL ABLATION ALL (3.5 hrs, 4 variants) — PASTE AS ONE CELL =====
# Keep this Colab open 3.5 hrs. Will save to Drive/ablation_REAL_...
%cd /content/H-CoAtNet-AStar
!pwd; !mkdir -p ablation tools
!wget -q https://raw.githubusercontent.com/Cyrax321/H-CoAtNet-AStar/main/ablation/ablation.py -O ablation/ablation.py && echo "✅ ablation.py"
!wget -q https://raw.githubusercontent.com/Cyrax321/H-CoAtNet-AStar/main/tools/generate_all_figures_COMPLETE.py -O tools/generate_all_figures_COMPLETE.py && echo "✅ figures"
!ls -lh ablation/ablation.py
!python ablation/ablation.py --variant all --epochs 30 --seed 42
# Save to Drive
import pathlib, datetime, shutil, subprocess
from pathlib import Path
DATE=datetime.datetime.now().strftime("%Y%m%d_%H%M")
OUT=Path(f"/content/drive/MyDrive/ablation_REAL_{DATE}")
OUT.mkdir(parents=True, exist_ok=True)
for p in Path("figures").glob("*"): shutil.copy(p, OUT/p.name)
for p in Path("ablation").glob("*"): 
    if p.is_file(): shutil.copy(p, OUT/p.name)
for p in Path("results").glob("*.json"): shutil.copy(p, OUT/p.name)
for p in Path("results").glob("*.tex"): shutil.copy(p, OUT/p.name)
print(f"✅ ABLATION DONE → {OUT} — {len(list(Path('figures').glob('*.png')))} PNGs")
subprocess.run(["ls","-lh",str(OUT)], check=False)
