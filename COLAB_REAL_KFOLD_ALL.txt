# ===== COLAB B — REAL K-FOLD 5-FOLD (4.5 hrs) — PASTE AS ONE CELL IN 2ND COLAB =====
# Keep this Colab open 4.5 hrs in parallel with COLAB A. Will save to Drive/kfold_REAL_...
%cd /content/H-CoAtNet-AStar
!pwd; !mkdir -p kfold tools
!wget -q https://raw.githubusercontent.com/Cyrax321/H-CoAtNet-AStar/main/kfold/kfold_hcoatnet.py -O kfold/kfold_hcoatnet.py && echo "✅ kfold.py"
!wget -q https://raw.githubusercontent.com/Cyrax321/H-CoAtNet-AStar/main/tools/generate_all_figures_COMPLETE.py -O tools/generate_all_figures_COMPLETE.py && echo "✅ figures"
!ls -lh kfold/kfold_hcoatnet.py
!python kfold/kfold_hcoatnet.py --k 5 --epochs 30 --seed 42
# Save to Drive
import pathlib, datetime, shutil, subprocess
from pathlib import Path
DATE=datetime.datetime.now().strftime("%Y%m%d_%H%M")
OUT=Path(f"/content/drive/MyDrive/kfold_REAL_{DATE}")
OUT.mkdir(parents=True, exist_ok=True)
for p in Path("kfold").glob("*"): 
    if p.is_file(): shutil.copy(p, OUT/p.name)
for p in Path("figures").glob("fig_kfold*"): shutil.copy(p, OUT/p.name)
for p in Path("results").glob("kfold*.json"): shutil.copy(p, OUT/p.name)
for p in Path("results").glob("*.json"): shutil.copy(p, OUT/p.name)
print(f"✅ K-FOLD DONE → {OUT}")
subprocess.run(["ls","-lh",str(OUT)], check=False)
